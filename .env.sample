# Copy to `.env`, which Git ignores. Never commit real values.

# Read by scripts/release.sh (`just release vX.Y.Z`): the Homebrew formula
# file to update after publishing, in your local clone of the tap.
HOMEBREW_FORMULA_FILE=/path/to/homebrew-tap/Formula/cli-bot.rb
